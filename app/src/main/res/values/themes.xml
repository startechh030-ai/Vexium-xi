<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme — full screen, no action bar, black background -->
    <style name="Theme.Obris" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Splash theme — Android 12+ splash screen API -->
    <style name="Theme.Obris.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FF000000</item>
        <item name="postSplashScreenTheme">@style/Theme.Obris</item>
    </style>
</resources>
